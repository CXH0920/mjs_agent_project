# 名将杀 Agent 打包发版指南

本项目用 PyInstaller 打包为 Windows 可执行文件，支持精简 / 完整双模式：

| 模式 | 命令 | 体积 | 功能 |
|---|---|---|---|
| 精简版（默认） | `python release.py --zip` | ~834 MB | 核心对战辅助（资料库 / 选将推荐 / 对局攻略）；AI 生成走经典模式（无 RAG 检索，见踩坑 15） |
| 完整版 | `python release.py --full --zip` | ~1.6 GB | 精简版 + **RAG 检索增强生成**（bge 模型 + chroma 索引随包）+ 知识库维护页 + Playwright 浏览器抓取 |

完整版比精简版大主要来自：playwright 包 + node driver（~122MB，用系统 Edge 不下载浏览器二进制）+ **RAG 推理底座**（torch CPU ~430MB + transformers ~110MB + chroma 索引 50MB + bge 模型 ~95MB，踩坑 15）。打包由 `release.py`（发版编排）+ `mjs_agent.spec`（PyInstaller 规格）完成，以下均来自实际发版验证。**改动相关代码时切勿回退踩坑处理，否则问题复现。**

打包边界（详见"四、注意事项 → 敏感内容与资料打包边界"）：
- **敏感内容一律不打包**：`config.env`（真实 Key）、`config/api_profiles.json`（多供应商 API 档案）、`data/edge_profile`（浏览器登录态）、`logs/`、用户层 `config/ocr_rois.json`、运行时状态文件
- **运行资料全量打包**：核心资料库 json、官方榜单 csv、RAG 权威语料（`data/rag_corpus/`）、评估集（`data/rag_evals/`）、`raw_guides/`、元规则母本、docs/prompts、templates/images 等；首启由 `_ensure_clean_runtime` 部署到 exe 同级（维护脚本读 `PROJECT_ROOT/data`，不部署会报"缺源"）

---

## 一、产物与环境

### 产物
- `dist/mjs_agent/mjs_agent.exe` — 启动器（精简 ~39MB / 完整 ~94MB），windowed 模式（无控制台窗口）
- `dist/mjs_agent/_internal/` — 依赖与资源（paddle / cv2 / PySide6 / OCR 模型 / torch + transformers + bge 模型 + chroma 索引（完整版）/ 数据等）
- 总体积：精简 ~834MB / 完整 ~1666MB（2026-08 实测，随依赖版本漂移略有浮动）；`--zip` 另产出 ~356MB(精简)/~730MB(完整) 分发包
- 纯净：不含 `config.env` / `config/api_profiles.json` / `data/edge_profile` / `logs` / api key（首启由程序生成）

### 运行环境
- conda `myenv`（`environment.yml`，Python 3.11）
- 额外 `pip install pyinstaller`（不在 environment.yml 内）
- PaddleOCR 模型：`~/.paddleocr/whl/{det,rec,cls}`（首次在 myenv 运行 PaddleOCR 自动下载）
- CPU 版 paddlepaddle 2.6.2：`release.py` 的 `prepare_build_deps()` 自动安装到 `build_deps/`（独立目录，不碰 myenv GPU 版）
- playwright（仅完整版）：`pip install playwright`；项目用 `channel="msedge"` 系统 Edge，**不需 `playwright install` 下载浏览器**
- RAG 推理栈（仅完整版打包需要）：myenv 内 torch（CPU 版，2.12.0+cpu 实测）/ transformers / tokenizers / sentence_transformers / chromadb 已随 environment.yml 装好，spec 直接收集，无需额外安装；bge 模型与 chroma 索引取自项目 `data/rag_models`、`data/rag_index`（开发态维护管道产物）

### 用法
```bash
conda activate myenv
python release.py --zip           # 精简版（构建+校验+烟雾+zip）
python release.py --full --zip    # 完整版（含 RAG + Playwright）
python release.py --no-smoke      # 跳过 22s 启动烟雾测试
python release.py --skip-build    # 跳过构建，只校验已有 dist
```

> ⚠️ **整体拷贝** `dist/mjs_agent` 文件夹到目标位置再双击，不要只拷 exe（exe 依赖同级 `_internal/`）。

---

## 二、打包流程（release.py 编排）

1. **预检** `preflight()`：spec、PyInstaller、PaddleOCR 模型存在、（完整版）playwright 已装；取 git 版本号
2. **准备 CPU paddle** `prepare_build_deps()`：`build_deps/paddle` 不存在则 `pip install --target`
3. **构建** `build()`：清 build/dist → `PyInstaller --clean`（`PYTHONPATH=build_deps` 让 collect_all 优先命中 CPU paddle；`--full` 设 `MJS_FULL=1`）
4. **纯净度校验** `purity_check()`：确认无 config.env/edge_profile/logs；显式断言 `_internal/config/` 无 `api_profiles.json`（含真实 Key 的敏感档案，出现即终止发版）；对**整个交付目录**（exe 根 + `_internal`）扫描 `sk-` 开头 api key 字面量
5. **完整版产物校验** `verify_full_artifacts()`（仅 `--full`）：`_internal/playwright/driver` 已收集 + `.full_build` 标记写入
6. **烟雾测试** `smoke_test()`：`os.startfile` 双击模式启动 22s，`tasklist` 查存活 + 读 logs 判致命错误；smoke 前清 `%TEMP%\mjs_ocr_models` 确保验证新模型
7. **zip** `make_zip()`（`--zip`）：打成分发包
8. **汇总** `report()`：体积 + 注意事项

---

## 三、打包过程问题与解决（踩坑）

> 以下每条都是实际发版中遇到、已修复的问题。

### 1. PyInstaller 6.x 不再分流裸 Tree 对象
- **现象**：`Analysis(datas=[Tree(...)])` 报 `ValueError: too many values to unpack (expected 2)`。
- **原因**：PyInstaller 6.x 的 `format_binaries_and_datas` 直接 `for src,dst in datas` 解包，不再 `isinstance(Tree)` 分流；而 `Tree` 继承 `TOC`，元素是三元组 `(name,path,kind)`，解包成 2 个变量即崩。
- **解决**：spec 用 `_collect_dir()`（walk + 过滤生成二元组）替代裸 `Tree`。data 目录的 excludes（edge_profile / char_info_cache / rag_* 等）在 `_collect_dir` 内按 basename / 扩展名 / 目录段过滤。

### 2. NavigationRail PAGE_LABELS 未随裁剪同步
- **现象**：启动崩 `icons 必须包含 4 个图标`；开发态正常。
- **原因**：UI 守卫裁了 `PAGE_CONTEXTS` / `navigation_icons`，但 `NavigationRail.PAGE_LABELS`（类常量 4 个）未同步裁剪，`__init__` 校验 `len(icons)==len(PAGE_LABELS)` 时不等崩溃。
- **解决**：`shell_widgets.NavigationRail` 用 `is_full_build()` 控制 PAGE_LABELS——完整版 4 页，精简版 3 页，使 PAGE_CONTEXTS / navigation_icons / PAGE_LABELS / Tab 创建四处同步。

### 3. Cython Utility/*.cpp 数据未收集
- **现象**：OCR 加载失败 `FileNotFoundError: ...\_internal\Cython\Utility\CppSupport.cpp`。
- **原因**：paddle 的 `paddle.utils.cpp_extension` import 时经 `setuptools.command.build_ext` 触达 `Cython.Compiler`，Cython 初始化按 `utility_dir` 读 `Utility/*.cpp`；PyInstaller 默认不收集 Cython 数据文件。
- **解决**：spec 加 `cython_datas = collect_data_files("Cython")`，并入 Analysis datas。

### 4. imageio dist-info 元数据缺失
- **现象**：补 Cython 后报 `No package metadata was found for imageio`，paddle 整条 import 失败。
- **原因**：paddleocr 依赖链运行时用 `importlib.metadata` 查 imageio 版本，但 imageio 的 `.dist-info` 未收集。
- **解决**：spec 加 `imageio_meta = copy_metadata("imageio")`。

### 5. cnradical 数据 + char_info_cache/wubi86 路径
- **现象**：OCR 加载成功后逐字刷 `cnradical 查询失败 ...dictionary.pickle`、`五笔86全码表不存在`。
- **原因**：(a) cnradical 包的 `data/dictionary.pickle` 未收集；(b) `character_feature_repository` 的 `DEFAULT_WUBI_TABLE` / `DEFAULT_CHARACTER_FEATURE_CACHE` 用 `PROJECT_ROOT/src/data`，frozen 下 src 在 `_internal`（BUNDLE_ROOT），路径错；且 spec 未收集 `src/data/char_info_cache.json`、`wubi86.txt`。
- **解决**：spec 加 `cnradical_datas = collect_data_files("cnradical")` + 收集 `src/data/{char_info_cache.json,wubi86.txt}` 到 `_internal/src/data`；代码改 `_SRC_DATA = (BUNDLE_ROOT if frozen else PROJECT_ROOT)/"src"/"data"`。命中预计算缓存后免逐字查 cnradical。

### 6. windowed 模式 sys.stdout 为 None
- **现象**：双击 exe 崩 `AttributeError: 'NoneType' object has no attribute 'reconfigure'`（main.py）；但烟雾测试通过。
- **原因**：spec `console=False`（windowed），双击无控制台 → `sys.stdout/stderr=None`；原代码 `sys.stdout.reconfigure()` 崩。
- **解决**：main.py 加 None 守卫 `if sys.stdout is not None: sys.stdout.reconfigure(...)`（stderr 同理）。

### 7. 烟雾测试用 PIPE 掩盖双击崩溃
- **现象**：问题 6 中烟雾测试通过、双击却崩。
- **原因**：原 smoke 用 `subprocess.Popen(stdout=PIPE)`，给 exe 接了管道（stdout 非 None），掩盖了双击场景（stdout=None）。
- **解决**：smoke 改用 `os.startfile`（模拟 explorer 双击，stdout=None），靠 `tasklist` 查存活 + 读 `logs/` 判致命错误。**改任何启动期代码后，务必用 startfile smoke 验证双击**，不要退回 Popen(PIPE)。

### 8. 中文路径（paddle + cv2 的 ANSI fopen）
- **现象**：exe 放中文路径（如 `G:\…\测试\…`）→ OCR `Cannot open file ...det\inference.pdmodel`、模板无法保存、模板窗口状态异常。
- **原因**：paddle C++ `ifstream` 与 cv2 `imread/imwrite` 都用 ANSI fopen，不支持中文路径。
- **解决**：
  - paddle（`paddle_loader.py`）：frozen 下把 `BUNDLE_ROOT/paddleocr_models` 复制到 `%TEMP%\mjs_ocr_models`（英文），det/rec/cls 指向它；仅当 `%TEMP%` 为纯 ASCII 时有效，否则回退 BUNDLE_ROOT。
  - cv2（`template_manager.py`）：`cv2.imread` → `open(rb)+cv2.imdecode(np.frombuffer(...))`；`cv2.imwrite` → `cv2.imencode + open(wb).write`。Python 的 open 支持 Unicode 路径。
  - 模板"窗口不一样"是 imread 失败→模板未加载→状态显示不同的连锁，**非 UI 裁剪**。
- **限制**：paddle 的 ifstream 无法彻底 workaround，OCR 仍依赖 `%TEMP%` 为纯 ASCII（即系统用户名为英文）。最稳妥仍是放纯英文路径。

### 9. 体积优化（1342MB → 834MB）

| 优化项 | 收益 | 做法 |
|---|---|---|
| torch / torchvision / transformers / tokenizers / onnxruntime | ~430MB | spec excludes（paddleocr.ppstructure 依赖，OCR 仅 det+rec 不用） |
| CPU paddle 替代 GPU 版 | ~500MB | `build_deps` 装 CPU paddlepaddle 2.6.2，`PYTHONPATH` 优先收集 |
| PySide6 WebEngine / QML | ~150MB | excludes（markdown 用 QTextBrowser，零 WebEngine） |
| upx | — | 关闭（paddle .pyd 压缩易致加载失败） |

剩余大头（paddle 274MB / cv2 123MB / PySide6 92MB / scipy 52MB / mkldnn 38MB）全是 OCR + GUI 核心，再裁风险高、收益低。

### 10. playwright driver（完整版）
- spec `collect_all("playwright")` 收集包 + node driver 到 `_internal/playwright/driver/`
- 用 `channel="msedge"` 系统 Edge，**不下载浏览器二进制**
- **运行期 `browser_session`（Edge 启动 + DeepSeek 抓取）需手动验证**：烟雾测不到（`browser_session` 是 lazy import，不在启动链；烟雾只验证启动 + RAG 页 import + driver 收集）
- 验证方法：双击完整版 exe → 知识库维护页 → 触发 AI 浏览器抓取 → 确认 Edge 启动 + 登录 + 回复提取正常

### 11. QProcess sys.executable frozen 重入（子进程调起自己）
- **现象**：frozen 下触发 AI 生成 / 维护页脚本执行后，启动**另一个 mjs_agent.exe 实例**（GUI），关闭它后提示"执行完毕"但脚本逻辑没跑、无数据返回。
- **原因**：frozen 下 `sys.executable` 不再是 python，而是 `mjs_agent.exe` 自己。代码里 `QProcess.start(sys.executable, args)` 会重入 exe（exe 无脚本模式，直接启动 GUI）。涉及两类调用：
  - **`-m` 模块**（`base_fetch_service:137`，AI 攻略/相性/武将生成跑 `sys.executable -m src.scraper.ai_batch`）——**已修**
  - **脚本文件**（`rule_doc_panel:63/750`、`rag_maintenance_panel:66/405`、`card_points_panel:482` 跑 `sys.executable scripts/xxx.py`）——**未修（待实施）**
- **解决（`-m` 模块，已修）**：
  - main.py 入口加 `-m` 分发：frozen 下 `argv[1]=="-m"` → `runpy.run_module(argv[2])`（模块从 PYZ 加载，不需文件系统 .py）；开发态 `IS_FROZEN=False` 不触发，python 自己处理 `-m`
  - spec `hiddenimports` 显式声明 `-m` 模块（`src.scraper.ai_batch` / `src.scraper.official` / `src.scraper.incremental` / `src.scraper.ai.browser_generator`）——PyInstaller 不跟踪 `-m` 字符串字面量，会漏收
- **解决（脚本文件，已随脚本迁移解决）**：维护脚本已从根目录 `scripts/` 迁移到 `src/scripts/` 包（根目录 `scripts/` 已不存在），维护面板（`rule_doc_panel` / `rag_maintenance_panel` / `card_points_panel`）全部改为 `-m src.scripts.xxx` **模块方式**调用（QProcess + `-m`），走 main.py 的 `-m` runpy 分发 + spec `collect_submodules("src.scripts")` 显式收集。原"收集 scripts/ 目录 + runpy.run_path 文件分发"的三步方案不再需要。
- **验证**：AI 生成——双击 exe → 触发浏览器/API 抓取 → 确认不启动另一个 exe + 数据正常返回。完整版维护页：触发规则文档审计 / RAG 维护 / 卡牌点数迁移 → 确认日志输出在面板内、不另启 GUI 实例。

### 12. config/api_profiles.json（多供应商 API 档案）被打进包
- **现象**：`purity_check` 的 sk- 扫描在 `_internal/config/api_profiles.json` 命中真实 Key，发版终止。
- **原因**：2026-08 引入多供应商 API 档案机制（`config/api_profiles.json`，`.gitignore` 标注敏感不入库），但 spec 的 config 收集 `excludes` 只排除了 `ocr_rois.json`，含真实 Key 的档案被 `_collect_dir` 一并收集进 `_internal/config/`。
- **解决**：
  - spec：config 收集 `excludes` 加 `api_profiles.json`（运行时程序从 `PROJECT_ROOT/config/` 读、缺失时 `load_api_profiles` 返回空档案，首启 `migrate_legacy_api_config` 按 config.env 生成默认档案，Key 为空——排除不影响功能）。
  - release.py：`purity_check` 显式断言 `_internal/config/api_profiles.json` 不存在（不依赖 sk- 模式扫描的巧合命中，Key 换格式也能拦）；sk- 扫描范围从 `_internal` 扩为**整个交付目录**，覆盖 exe 根的 `config.env` / `config/`。
- **验证**：发版完成后确认 `_internal/config/` 只有 faction_colors / model_pricing / ocr_rois.default；exe 同级 `config/api_profiles.json` 只在首启后由程序生成。

### 13. 核心规则摘要.md 被 rag_corpus 整目录排除连带漏收
- **现象**：frozen 下 AI 生成无 RAG 时的"卡牌体系防串味兜底"失效（`rule_summary` 加载失败，降级为空）。
- **原因**：`rule_summary.py` 从 `BUNDLE_ROOT/data/rag_corpus/核心规则摘要.md` 读（主链路、只读资源）；spec 为排除可重建的 RAG 语料/索引把 `rag_corpus` 整目录列入排除名单，把这份 **git 跟踪的资料**连带漏掉了。
- **解决**：spec 在 data 遍历后**显式补收** `data/rag_corpus/核心规则摘要.md`（与 src/data 基线同样的处理方式）。rag_corpus 其余语料当时仍排除，后续随踩坑 14 全量入包。

### 14. 知识库维护页全量"缺源"（维护脚本读 PROJECT_ROOT，资料未部署）
- **现象**：完整版 exe 打开知识库维护页，语料状态全部显示"缺源"；触发语料重建报 `❌ 缺少源数据 ...`。
- **原因**（两个独立缺陷叠加）：
  1. **资料未部署**：维护面板 `task_states` 与 build 脚本（`rag_common.ROOT` 等）全部读 `PROJECT_ROOT/data`（exe 同级可写根），而打包资料只进了 `_internal/data`（BUNDLE_ROOT）；`_ensure_clean_runtime` 首启只 mkdir 空 `data/` 不复制内容 → 源数据、语料全部"不存在"。
  2. **spec 漏收**：`rag_corpus` 语料（除核心规则摘要）与 `rag_evals` 评估集被当"可重建内容"排除；元规则/术语/FAQ 任务的源 `docs/元规则整理-完整版.md` 完全不在收集范围（docs 只收了 prompts）。
- **解决**：
  1. spec：DATA_NAMES 移除 `rag_corpus` / `rag_evals`（权威语料与评估集全量入包，仅 2.2MB+12KB；`rag_index`/`rag_models` 仍排除）；单独收集 `docs/元规则整理-完整版.md`。
  2. `main._ensure_clean_runtime`：首启把 `BUNDLE_ROOT/data` 逐文件部署到 exe 同级 `data/`、元规则母本部署到 exe 同级 `docs/`（只补缺失，不覆盖用户已有数据——用户后续用维护页重建/导入的更新不会被首启覆盖）。
- **验证**：双击完整版 exe → 知识库维护页：语料状态应为"最新/待重建"（无"缺源"）；`mjs_agent.exe -m src.scripts.maintain_rag --check` 应能读到全部源数据。

### 15. 分发版 AI 生成从未真正进过 RAG 模式（静默降级，三处叠加）
- **现象**：分发版生成攻略/相性不带 RAG 语料注入（经典模式），**无任何报错**；开发态正常。语料/索引/模型即使修好部署（踩坑 14）也无济于事。
- **原因**（三处叠加，缺一不可）：
  1. **推理底座被排除**：踩坑 9 体积优化 excludes 了 `torch/transformers/tokenizers`（-430MB），连带切掉 sentence_transformers 的依赖——PYZ 里 sentence_transformers 167 个模块都在，但 transformers 0 个，`Retriever.model` 的 `from sentence_transformers import SentenceTransformer` 必然 ModuleNotFoundError。
  2. **模型与索引未打包**：`data/rag_models`（bge 模型）与 `data/rag_index`（chroma 向量索引）此前整体排除，检索无数据可查。
  3. **stdout 守卫缺失**：`retriever.py` 顶层 `sys.stdout.reconfigure(...)` 无 None 守卫——windowed 双击场景 stdout 为 None（踩坑 6 同类），import 模块即 AttributeError。
  - 降级路径：`rag_prompt.build_rag_context` 的 try/except 按设计"任何异常一律降级为空串"，所以三层故障都**静默**退回经典模式，不写致命日志。
- **解决**：
  1. spec：完整版 `--full` 从 excludes 移除 torch/transformers/tokenizers（torchvision/onnx/onnxruntime 仍排除——sentence_transformers 用 torch 推理，chromadb 默认 embedding 用不到，检索显式传 query_embeddings）；收集 chroma 索引与 bge safetensors（见"边界清单"）。
  2. `retriever.py`：stdout reconfigure 加 None 守卫。
  3. 精简版维持原状（RAG 降级为经典模式是精简版的预期行为，语料仍随包供维护页用）。
- **体积代价**：完整版 +~630MB（torch ~430MB + transformers ~110MB + 索引 50MB + 模型 95MB，模型去 bin 省 ~90MB）。
- **验证**：`mjs_agent.exe -m src.scripts.eval_rule_faqs --limit 3` —— 真实端到端检索（加载 bge 模型 → chroma query → 语料命中断言，零 LLM 成本）；通过标准：打印评估报告、无 ModuleNotFoundError / 模型加载失败。

---

## 四、注意事项

### exe 路径
- 中文路径已支持（见踩坑 8），但 OCR 依赖 `%TEMP%` 为纯 ASCII（系统用户名英文）。若系统用户名含中文，OCR 模型仍可能加载失败——**最稳妥仍是纯英文路径**。
- 分发时建议说明"请放英文路径"。

### %TEMP% OCR 模型缓存
- 首启把 `paddleocr_models` 复制到 `%TEMP%\mjs_ocr_models`，后续启动复用缓存。
- **更新 `paddleocr_models` 并重新发版后，需删 `%TEMP%\mjs_ocr_models`** 让程序重新复制（否则用旧模型）。`release.py` 的 smoke 会自动清它以验证新模型，但用户机器上的缓存不会自动清。

### 首启运行时文件
- exe 同级首启生成：`config.env`（API Key 留空注释，核心对战辅助无需填）、`data/`（含 `edge_profile` 浏览器缓存）、`logs/`、`config/`、`templates/`、`images/`、`mjs.ico`。
- `data/` 还会从 `_internal/data` **部署全部打包资料**（核心库 json / 榜单 csv / RAG 语料 / 评估集 / raw_guides），`docs/元规则整理-完整版.md` 同理——只补缺失文件，不覆盖用户已有数据（踩坑 14）。
- `config/api_profiles.json`（API 档案）由 `migrate_legacy_api_config` 按 config.env 旧键生成（打包版 Key 为空），或用户在设置对话框手工添加。
- 由 `main._ensure_clean_runtime` 从 `_internal`（BUNDLE_ROOT）复制，**不覆盖用户已有数据**。

### 敏感内容与资料打包边界
**敏感/用户数据一律不打包**（构建产物纯净，首启由程序在 exe 同级生成）：

| 文件 | 敏感点 | 排除机制 |
|---|---|---|
| `config.env` | 真实 API Key、本机 ADB 路径 | spec 只收 `config.env.example`；首启复制 example 生成 |
| `config/api_profiles.json` | 多供应商真实 API Key（.gitignore 标注敏感） | spec config excludes（踩坑 12）；运行时读 `PROJECT_ROOT/config/`、缺失返回空档案，首启 migrate 生成空 Key 档案 |
| `config/ocr_rois.json` | 用户层 ROI 个人配置 | spec config excludes；首启从 `ocr_rois.default.json` 复制 |
| `data/edge_profile/` | Edge 浏览器登录态（~107MB） | spec data 排除；首启由程序生成 |
| `logs/`、`data/backups/` | 本机运行痕迹 | spec data 排除 |
| `data/{announcements,baike_snapshot,武将推荐指数状态,char_info_cache}.json` | 运行时状态/缓存 | spec DATA_NAMES 排除 |

**运行资料全量打包**：核心资料库 `heroes/cards/combos/synergies/guides/card_points/equip_attrs/hero_classification/mjs_adjustments.json`、官方榜单 csv（`2v2出场排行/2v2胜率排行/巅峰赛出场排行/巅峰赛胜率排行/武将放逐/武将推荐指数.csv`，选将推荐/巅峰赛仓库的 `BUNDLE_ROOT/data` 数据源）、**RAG 权威语料**（`data/rag_corpus/` 全部——既是 `rule_summary` 防串味兜底的资料，也是语料任务的输出与跨语料联动源（如装备属性语料依赖 `卡牌RAG语料.json`）；注意多数语料未入 git，仅存在于打包工作区）、**评估集**（`data/rag_evals/`，FAQ 评估用）、`raw_guides/`、`docs/元规则整理-完整版.md`（元规则/术语/FAQ 语料任务的源）、`docs/prompts/`、`templates/`、`images/`、`src/data/{char_info_cache.json,wubi86.txt}` 基线。

**可重建/废弃内容不打包**（省体积）：`eval_*/syn_*/sample_*/test_guide`（评估临时产物）、`example_*.json`（已废弃无引用）、`武将分类20*.md`（一次性分析）、`*_待复核.csv`、`archive/`。

**RAG 向量检索数据（踩坑 15，仅完整版 `--full` 打包）**：`data/rag_index/`（chroma 50MB 向量索引）+ `data/rag_models/modelscope/models/BAAI--bge-small-zh-v1.5`（bge 模型 HF snapshots 结构，只收 safetensors 权重 ~95MB，排除冗余 `pytorch_model.bin`）。精简版仍排除——torch/transformers/tokenizers 被 excludes 后检索引擎不存在，索引/模型收了也无法消费，AI 生成静默降级为经典模式。运行期随资料部署复制到可写根（chroma 打开 sqlite 需写权限，不能留在只读 `_internal`）。

**首启部署**（`main._ensure_clean_runtime`，踩坑 14）：维护脚本/构建脚本读 `PROJECT_ROOT/data`（exe 同级），而打包资料在 `_internal/data`（BUNDLE_ROOT）——首启把 BUNDLE_ROOT/data 全量复制到 exe 同级 `data/`、元规则母本复制到 exe 同级 `docs/`（只补缺失文件，不覆盖用户已有数据）。

**发版前校验**（`purity_check()`）：显式断言 `_internal/config/` 无 `api_profiles.json`；对整个交付目录递归扫描 `sk-` 开头 20+ 位 api key 字面量（文本文件），命中即终止发版。

### 功能裁剪（双模式）
- **精简版**：知识库维护页（RAG 语料/索引维护）移除，避免打包 playwright + RAG 依赖链；torch/transformers/tokenizers 仍排除 → **AI 生成走经典模式（无 RAG 检索，踩坑 15）**。核心对战辅助（资料库 / 选将推荐 / 对局攻略）+ OCR + AI 生成（httpx 抓取，不触达 playwright）完整。
- **完整版**（`--full`）：保留知识库维护页 + Playwright 浏览器抓取 + **完整 RAG 检索**（推理底座 + chroma 索引 + bge 模型随包）。UI 守卫用 `is_full_build()` 控制（完整版保留 4 页，精简版裁 3 页）。
- 切换不影响业务代码——靠 `MJS_FULL` 环境变量 + `.full_build` 标记，同一份代码在两模式间切换。

### 烟雾测试
- 用 `os.startfile` 双击模式（非 Popen PIPE），能捕获双击场景崩溃（如 stdout=None 类）。
- 靠 `tasklist` 查进程存活 + 读 `logs/` 判致命错误（Traceback / 模型加载失败 / No module named）。
- GUI 正常运行 22s 后被 taskkill，视为通过。

---

## 五、spec 关键设计（mjs_agent.spec）
- **onedir 模式**：启动快、便于排错；产物 `dist/mjs_agent/mjs_agent.exe` + `_internal/`。
- **双模式开关**：顶部 `FULL = os.environ.get("MJS_FULL") == "1"`；完整版不 exclude playwright/RAG 推理底座 + `collect_all("playwright")` + 收 chroma 索引/bge 模型 + 写 `.full_build` 标记；精简版 exclude 这些。
- **CPU paddle 收集**：`BUILD_DEPS` 优先 `sys.path.insert(0)`，`collect_all("paddle")` 命中 CPU 版；`release.py` 另设 `PYTHONPATH=build_deps` 双保险。
- **OCR 模型**：从 `~/.paddleocr/whl` 取 det/rec/cls ch 子集，置入 `paddleocr_models/{det,rec,cls}` 离线用；运行时由 `paddle_loader` 复制到 `%TEMP%` 规避中文路径。
- **静态资源**：`_collect_dir` 收集 config/templates/images 到 `_internal`（config excludes 用户层 `ocr_rois.json` + 敏感 `api_profiles.json`），data 遍历用 `_data_excluded` 过滤运行时产物（RAG 语料/评估集入包）；`docs/元规则整理-完整版.md` 与 `src/data/{char_info_cache.json,wubi86.txt}` 显式补收；完整版另收 chroma 索引与 bge safetensors。
- **excludes**（精简版）：Qt WebEngine/QML、torch/transformers/tokenizers（RAG 推理底座，踩坑 15）、playwright、onnx/onnxruntime、测试工具等；完整版仅移除 torch/transformers/tokenizers/playwright 四项排除。
- **windowed**：`console=False`，`upx=False`（paddle .pyd 不压缩）。

### 双模式机制
- **构建期**：`release.py --full` 设 `MJS_FULL=1` → spec `FULL` 开关 → 动态 excludes + `collect_all("playwright")` + 写 `.full_build` 进 `_internal/`
- **运行期**：`env.is_full_build()` 开发态恒 True；frozen 下读 `BUNDLE_ROOT/.full_build` 标记 → UI 守卫据此决定保留/裁剪知识库维护页

---

## 六、故障排查

| 现象 | 原因 | 处理 |
|---|---|---|
| 双击一闪而过 / `reconfigure NoneType` | windowed 下 sys.stdout=None | 检查 main.py None 守卫；用 startfile smoke 复现 |
| OCR `Cannot open file ...pdmodel` | 中文路径 / 模型未打包 | 中文路径靠 %TEMP% workaround；检查 spec ocr_model_datas + `~/.paddleocr` |
| 模板无法保存 / 窗口状态异常 | cv2 imread/imwrite 中文路径 | 检查 template_manager 是否用 imdecode/imencode |
| 烟雾过但双击崩 | smoke 用 Popen(PIPE) 掩盖 | 确认 smoke 是 os.startfile 模式 |
| `icons 必须包含 4 个图标` | PAGE_LABELS 未随裁剪同步 | 检查 shell_widgets 的 is_full_build 分支 |
| `No package metadata ... imageio` | imageio dist-info 未收集 | 检查 spec `copy_metadata("imageio")` |
| `Cython/Utility/CppSupport.cpp` | Cython 数据未收集 | 检查 spec `collect_data_files("Cython")` |
| `cnradical 查询失败` 刷屏 | cnradical 数据/char_info_cache 路径 | 检查 spec cnradical_datas + src/data 收集 + BUNDLE_ROOT 路径 |
| 体积过大 | torch 等未排除 | 检查 spec excludes（torch/transformers/onnxruntime） |
| 构建报 `too many values to unpack` | 裸 Tree 进 datas | 检查 spec 是否用 `_collect_dir` 替代 Tree |
| `browser_session` 报错（完整版） | playwright driver 路径/Edge 未装 | 检查 `_internal/playwright/driver`；确认系统有 Edge；手动触发浏览器抓取验证 |
| 纯净度报 `_internal/config/api_profiles.json` | spec config excludes 未生效 | 检查 spec `_collect_dir(config, excludes=[...])` 含 `api_profiles.json`，修后重新构建 |
| AI 生成 prompt 缺"卡牌体系"兜底段 | 核心规则摘要.md 未进包 | 检查 spec 显式补收 `data/rag_corpus/核心规则摘要.md` |
| AI 生成不带 RAG 语料（静默经典模式） | 完整版缺推理底座/索引/模型，或 stdout None | 确认用 `--full` 构建；`mjs_agent.exe -m src.scripts.eval_rule_faqs --limit 3` 端到端验证（踩坑 15）；检查 retriever stdout None 守卫 |

---

## 七、维护指引（何时改 spec）

### 日常迭代重打包 SOP（改完代码 → 出包）
1. 开发态先跑通测试（pytest）与冒烟功能
2. 一条命令出包（自动完成：预检 → 清 build/dist 干净构建 → 纯净度校验 → 烟雾测试 → zip）：
   - 完整版：`python release.py --full --zip`（实测 ~6-8 分钟，torch 收集较慢）
   - 精简版：`python release.py --zip`（~4-6 分钟）
3. 版本号取 `git describe`（tag 优先），发版前打好 tag 让 zip 文件名带版本
4. 验证清单（按本次改动范围选做，烟雾测试只覆盖启动链）：
   - 改了启动期代码（`main.py` / `env.py` / `shell_widgets`）→ **保留烟雾测试**，勿 `--no-smoke`
   - 改了 RAG 检索 → `mjs_agent.exe -m src.scripts.eval_rule_faqs --limit 3`（端到端真实检索，零 LLM 成本）
   - 改了知识库维护页 → 打包版人工触发一次对应功能
   - 改了浏览器抓取 → 完整版人工触发 AI 浏览器抓取（`browser_session` 是 lazy import，烟雾测不到）
5. 产物校验 `release.py` 已内置（纯净度 / api key 扫描 / playwright driver / `.full_build` 标记），无需手动检查

### spec 永不重建，只渐进修改
spec 固化了 15 条踩坑处理（PyInstaller 6.x Tree 兼容 / CPU paddle 优先 / OCR 模型与中文路径 / stdout 守卫 / chromadb 动态子模块全量收集…），**从零重写 = 全部回退、踩过的坑全部复现**。任何新需求都优先往现有结构里加/删几行；确需调整大结构（见下"要大改"）也只在现有 spec 上改。

### 不用动 spec（日常 90%）
- 改 bug、改业务逻辑、重构
- 加 `.py` 模块（标准 import + 现有包）→ PyInstaller 自动收集依赖
- 往 `templates/`、`images/` 加资源（spec 用 glob 自动收）
- 新功能读写文件用 `from src.config.env import PROJECT_ROOT, BUNDLE_ROOT`（已 frozen 适配）

### 要小改 spec
| 场景 | 改什么 |
|---|---|
| 加非 templates/images 的数据文件（新模型/独立 json 等） | spec `datas` 加一行 |
| 新增含敏感信息的本地配置（账号/Key/登录态文件） | spec 的 config excludes 或 data `_data_excluded` 加文件名，并在 `release.py purity_check` 加显式断言（参考踩坑 12） |
| `pip install` 新三方包且烟雾报 `ModuleNotFoundError` | spec `hiddenimports` 加模块名（多数情况自动收，不用加） |
| 包内运行时报 `No module named X`，且 X 是某包内部按需动态 import 的子模块（如 chromadb 的 telemetry/api.rust） | `collect_all("X")` 按文件系统全量兜住（踩坑 15） |
| 包内运行时报 `No package metadata was found for X` | `copy_metadata("X")`（踩坑 4 imageio） |
| 新功能用动态 import / 插件式加载 | `hiddenimports` 加模块名 |
| 新功能依赖 WebEngine | 去掉对应 excludes |

### 判断方法：构建自证，不用猜
spec 收集是否够，靠构建 + 烟雾 + 实测新功能路径验证：
- 报 `ModuleNotFoundError` / `No package metadata` / 数据文件 `FileNotFoundError` → 按上表对应处理
- 构建日志 `build/mjs_agent/warn-mjs_agent.txt` 记录了每个模块"收集/缺失/被 excludes"的原因，排查首选
- `excluded module named X` = 被 excludes 主动排除（精简版见到 torch/transformers 属正常）

### 要大改 spec 的场景（仍是在现有结构上改，非重建）
| 场景 | 改什么 |
|---|---|
| 入口移动/改名（`src/main.py` 换位置） | `Analysis([...])` 的脚本路径 |
| onedir 改 onefile | 删 `COLLECT`、EXE 合并 binaries/datas（牺牲启动速度与排错能力，不建议） |
| 新增第二个 exe / 子进程 exe | 增加 `EXE` + `COLLECT` 对 |
| 应用名 / 图标变更 | `EXE(name=, icon=)` |

### frozen 适配要点
新功能读写文件**别用 `Path(__file__).parent` 硬推根目录**，走 `env.py` 的：
- `PROJECT_ROOT`：可写运行时根（exe 同级，config.env/logs/用户缓存写入处）
- `BUNDLE_ROOT`：只读打包资源根（`_internal`，静态数据/模板/图片）

frozen 自动适配，不用额外改 spec。
