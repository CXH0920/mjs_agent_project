# Byte-compiled / optimized files
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# IDE
.idea/
.zcode/


# Project documentation
project_problem.md
review-report.md
refactor-plan.md
data/synergies.json
data/guides.json
data/*.corrupt-*.json
data/*_待复核.csv
# 官方榜单导入生成物（由官方导入/推荐指数计算生成）
data/2v2胜率排行.csv
data/2v2出场排行.csv
data/巅峰赛胜率排行.csv
data/巅峰赛出场排行.csv
data/武将放逐.csv
data/武将推荐指数.csv
# 推荐指数运行时状态（程序自动更新，避免反复出现无关 diff）
data/武将推荐指数状态.json
data/official_import_pending.json
# 汉字特征打包基线（character_feature_repository 读 BUNDLE_ROOT/src/data/，需入库供 spec 收集）
# 用户层缓存在项目根 data/char_info_cache.json，见下方忽略
# 武将分类带日期快照文档（一次性分析，源数据见 hero_classification.json）
data/武将分类20*.md
# 公告监控运行时产物（运行时自动生成/更新）
data/announcements.json
data/baike_snapshot.json
data/backups/
data/edge_profile/
PLANS.md
retrospective.md

# Environment files
config.env
config/ocr_rois.json
# API 档案含敏感 Key（多供应商/多账号配置，见 api_multi_config_design.md）
config/api_profiles.json

# Edge browser profile (Playwright browser mode)
data/edge_profile/

# OS
.DS_Store
Thumbs.db
screenshots/
screenshot_data/
logs/
# 本地真实截图回归样本(不入库;缺失时对应测试自动跳过)
tests/fixtures_local/

# RAG 生成材料（语料/向量索引/模型缓存均可由维护管道重建，不入库）
data/rag_models/
data/rag_corpus/*
!data/rag_corpus/核心规则摘要.md
data/rag_index/

# RAG 维护运行时状态（脚本已迁移到 src/scripts/，ROOT 为项目根；旧 scripts/ 前缀不再生效）
scripts/.rag_state.json
scripts/.rule_doc_snapshot.json
/.rag_state.json
/.rule_doc_snapshot.json
# 元规则数据段同步运行产物（sync_rule_stats.py 生成的差异报告/确认清单）
scripts/.sync_*.json
# pytest 临时目录（测试运行产物）
.tmp_test/
# 攻略/相性质量评估临时产物（验收用，可删）
data/eval_*.json
data/syn_*.json
data/sample_*.json
data/test_guide.json
data/eval_review.md
data/_skills_dump.txt
data/_syn_review.md
# 环境备份（pip 清单与 paddle/libs DLL，体积大且含机器相关内容，不入库）
env_backup/

# 打包构建产物与依赖（release.py 生成，每台机器独立，不入库）
build/
dist/
build_deps/
# 汉字特征用户层缓存（项目根 data/，运行时自动扩展）
data/char_info_cache.json

# 示例占位符数据（已被 prompt 内嵌 few-shot 示例替代）
data/example_guides.json
data/example_synergies.json
# UI 视觉基线截图（改造阶段对比，改造已完成）
docs/ui_baseline/*.png
# 归档提案（历史记录，按需保留本地）
docs/archive/proposals/CP-*.json

# scripts 运行时状态/快照（整合到 src/scripts/ 后生成于此）
src/scripts/.*.json
