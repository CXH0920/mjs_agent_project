# 选将推荐胜率前三视觉锚点

`RecommendationPanel._apply_medal_rankings()` 仍按胜率降序计算前三名，业务排序不变。前三名的视觉强化由 `HeroCardWidget.set_medal()` 完成：

- 第 1 名使用金色边框、浅金底色和 `TOP 1` 徽章。
- 第 2 名使用银灰色边框、浅灰底色和 `TOP 2` 徽章。
- 第 3 名使用铜色边框、浅橙底色和 `TOP 3` 徽章。
- 胜率文字同步加粗并使用对应排名色，普通卡片保持原样。

这样用户无需先阅读卡片内容，就能通过卡片边框、底色、徽章和胜率四个层级快速识别前三名。
